doctype html
html(lang='en')
  head
    title= title
    meta(charset='utf-8')
    link(rel='stylesheet',href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body(style='padding-bottom: 10rem;')
    .container
        block navigation
            nav.navbar.navbar-inverse(role="navigation")
                .navbar-header
                    button.navbar-toggle.collapsed( type="button", data-toggle="collapse", data-target="#navbar-default", aria-expanded="false", aria-controls="navbar")
                        span.sr-only
                        span.icon-bar
                        span.icon-bar
                        span.icon-bar
                    a.navbar-brand(href='#') Holdings Analysis

                #navbar-default.collapse.navbar-collapse
                    ul.nav.navbar-nav
                        li.active: a(href='/') Home
                        li
                            if(user)
                                a(href='/auth/logout') Logout
                            else
                                a(href='/auth/login') Login
                        li
                            if(user)
                                a(href='/uploads') Upload Holdings
                            else
                                a(href='/auth/register') Register
                        li
                            if(user)
                                a(href='/uploads/firmMaintain') Firms Maintain
        block content
        block footer
            div.footer
                |&copy; Sridhar Sathya 2017-
                script.
                    document.write(new Date().getFullYear())